const router = new Router();

const gh_form = document.querySelector("#gh_form");
// gh_form.addEventListener("submit", (e) => {
//   e.preventDefault();
//   const username = gh_form["username"].value;
//   router.route("/repos", () => {});
// });
